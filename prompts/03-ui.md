# Fase 3 — Interface

Implemente o wizard a partir de `spec/form-flow.json`.
Siga `product/brand-tokens.json`, `product/design-system.md`, `product/ui-requirements.md` e `product/ui-copy.md`.

Dark mode da Rota no aplicativo.
Preview do contrato em A4 branco.
Fallback textual do logo até o SVG oficial existir.
MOCK/LIVE sempre visível.

A UI não contém regra de negócio duplicada: chama o domínio.
